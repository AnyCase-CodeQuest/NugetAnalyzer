namespace NugetAnalyzer.DTOs.Models.Enums
{
    // Don't change the order
    public enum PackageDateStatus : byte
    {
        Undefined = 0,
        Info = 1,
        Warning = 2,
        Error = 3
    }
}
